name: Rizin
on: workflow_dispatch
  
jobs:
  rizin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run
        run: docker run -v "$PWD:/pwd" -e RZ_LOGLEVEL=0 -e RZ_LOGSHOWSOURCES=1 --entrypoint /bin/sh rizin/rizin:latest -c "rizin -q -c 'aa; pdg @ 0x8f01dc' /pwd/*.so" > upload.c

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          archive: false
          path: upload.c
