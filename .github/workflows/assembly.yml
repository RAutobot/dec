name: Assembly
on:
  workflow_dispatch:
    inputs:
      url:
        description: URL
        required: true
      symbols:
        description: Symbols

jobs:
  assembly:
    runs-on: ubuntu-latest
    steps:
      - name: Run
        run: |
          filename="$(curl -sSLOJ -w %{filename_effective} "${{ inputs.url }}")"

          llvm-objdump-18 -d --no-leading-addr --no-show-raw-insn ${{ inputs.symbols && format('--disassemble-symbols="{0}"', inputs.symbols) || '' }} "$filename" | llvm-cxxfilt-18 | sed -E '
            s/^[[:space:]]+//
            s/[[:space:]]+/ /g
            s/<(.+)>:$/\1/
          ' > demangled.txt

          llvm-objdump-18 -d --no-leading-addr --no-show-raw-insn ${{ inputs.symbols && format('--disassemble-symbols="{0}"', inputs.symbols) || '' }} "$filename" | sed -E '
            s/^[[:space:]]+//
            s/[[:space:]]+/ /g
            s/<(.+)>:$/\1/
          ' > mangled.txt

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          name: assembly
          path: assembly.txt
