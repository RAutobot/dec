name: Assembly
on:
  workflow_dispatch:
    inputs:
      url:
        description: URL
        required: true
      commands:
        description: Commands
        default: --no-leading-addr

jobs:
  assembly:
    runs-on: ubuntu-latest
    steps:
      - name: Run
        run: |
          filename="$(curl -sSLOJ -w %{filename_effective} "${{ inputs.url }}")"
          llvm-objdump-18 -d --no-show-raw-insn ${{ inputs.commands }} "$filename" | llvm-cxxfilt-18 | sed -E '
            s/^.*<(.+)>:$/\1/
            s/[ \t]+/ /g
            s/^[ \t]+//
          ' > assembly.txt

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          name: assembly
          path: assembly.txt
