name: Object Dump
on: workflow_dispatch
  
jobs:
  object-dump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run
        run: llvm-objdump-18 -d -C --start-address=0x8f5bc0 --stop-address=0x914b64 *.so | sed -E "s|\s+| |g; s|^[[:xdigit:]]+ <(.*)>:$|\1|; s|^ [[:xdigit:]]+: [[:xdigit:]]+ ||" > upload.txt

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          archive: false
          path: upload.txt
